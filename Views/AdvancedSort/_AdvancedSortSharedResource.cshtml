@* Namespace: Kendo.Component.AdvancedSort.Mvc *@
@using Kendo.Component.AdvancedSort.Mvc.Common
@using Kendo.Component.AdvancedSort.Mvc.Models
@model GridComponentConfigModel
@{
    var loadSharedResource = Convert.ToBoolean(HttpContext.Current.Items[AppConstant.AdvancedSortSharedResourceKey]);
    if (loadSharedResource)
    {
        @Html.LoadJavaScript("require.js").Deferred(true)
        @Html.LoadJavaScript("AdvancedSort.js").Deferred(true)
        @Html.LoadJavaScript("AdvancedSortRoot.js")
        @Html.LoadCss("main.css")
        @Html.ActionLink("GetAdvancedSortPopup", "GetAdvancedSortPopup", "AdvancedSort"
            , new { Area = "" }
            , new
            {
                @id = "GetAdvancedSortPopupId",
                @class = "hide",
                @data_error_title = "Sort Criteria",
                @data_error = "An error has occurred while adding Sort Criteria, Please try again."
            })
    }

    if (!Model.UseDefaultSortIconStyle)
    {
        <style type="text/css">
            /* override the grid sort icon styles to have them visible regardless of column width */
            #@Model.GridName .k-grid-header th.k-header {
                position: relative;
            }

            #@Model.GridName .k-grid-header th > .k-link > .k-sort-order {
                position: absolute;
                top: 33%;
                right: 24px;
            }

            #@Model.GridName .k-grid-header th > .k-link > .k-icon.k-i-sort-asc-sm,
            #@Model.GridName .k-grid-header th > .k-link > .k-icon.k-i-sort-desc-sm {
                position: absolute;
                top: 33%;
            }

            #@Model.GridName .k-grid-header th > .k-link > .k-icon.k-i-sort-asc-sm {
                right: 25px;
            }

            #@Model.GridName .k-grid-header th > .k-link > .k-icon.k-i-sort-desc-sm {
                right: 40px;
            }
        </style>
    }
}